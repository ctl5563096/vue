const baseApiAddress = 'http://127.0.0.1/api';

const apiBaseUrl = 'http://localhost';

const apiUrl = {
    // user api
    userApi : baseApiAddress + '/userInfo',
};

export default {apiUrl,apiBaseUrl};